import ReactDOM from 'react-dom'
import App from './App'
import './index.css'

// To render the app components and to get the root (given in index.html)
ReactDOM.render(<App/>, document.querySelector('#root'))